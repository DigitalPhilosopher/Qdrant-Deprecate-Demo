{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Docker Compose Up",
            "type": "shell",
            "command": "docker-compose up --build -d",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}